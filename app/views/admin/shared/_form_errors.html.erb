<% if entity.errors.any? %>
  <div id="error-explanation">
    <p>The form contains <%= pluralize(entity.errors.count, "error") %>:</p>
    <ul>
    <% entity.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
    </ul>
  </div>
<% end %>
